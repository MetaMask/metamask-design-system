# Figma Design Config

Session defaults for design-verification preflight. Update per task.

- **MMDS Components file:** https://www.figma.com/design/1D6tnzXqWgnUC3spaAOELN/%F0%9F%A6%8A-MMDS-Components
- **Local working file (optional):** `https://www.figma.com/design/YOUR_FILE_KEY/Your-File-Name`
  - Personal scratch files vary by designer — set your own URL here or pass `INSERT_PAGE_LINK` per session
  - **Requires:** Subscribe 🦊 MMDS Components library in Figma (Assets → Libraries) before component creation in a non-MMDS file
- **Fonts:** _(auto-detected from MMDS `Typography` collection)_
- **Session goal:** _(what are we designing today?)_

## Rules

1. Every visual value must bind to **MMDS** Styles or Variables — never `material-theme/*` or `Schemes/*`.
2. Prefer creating components in **MMDS Components** directly. A local working file is fine only with the MMDS library subscribed.
3. Never start designing before the Design Brief is confirmed (when using `reference-interpreter`).
4. For code → Figma component publishing, use `/figma-component-creation` with Figma Console MCP.
