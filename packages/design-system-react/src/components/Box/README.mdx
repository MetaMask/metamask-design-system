import { Controls, Canvas } from '@storybook/blocks';

import * as BoxStories from './Box.stories';

# Box

Box is a low level flexbox layout component. It provides a focused set of props that map to Tailwind flexbox utility classes for easy flex-based layout control.

<Canvas of={BoxStories.Default} />

## Props

### `flexDirection`

Use the `flexDirection` prop to configure the `flex-direction` style of the Box.

Available values:
- `BoxFlexDirection.Row` - Horizontal layout
- `BoxFlexDirection.RowReverse` - Horizontal layout, reversed order
- `BoxFlexDirection.Column` - Vertical layout
- `BoxFlexDirection.ColumnReverse` - Vertical layout, reversed order

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>BoxFlexDirection</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>BoxFlexDirection.Row</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.FlexDirection} />

### `flexWrap`

Use the `flexWrap` prop to configure the `flex-wrap` style of the Box.

Available values:
- `BoxFlexWrap.NoWrap` - Items stay on single line
- `BoxFlexWrap.Wrap` - Items wrap to new lines as needed
- `BoxFlexWrap.WrapReverse` - Items wrap to new lines in reverse order

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>BoxFlexWrap</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>BoxFlexWrap.NoWrap</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.FlexWrap} />

### `gap`

Use the `gap` prop with values from the `BoxSpacing` scale (0–12) to configure the gap between flex items. Each unit equals 4px.

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>BoxSpacing</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>0</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.Gap} />

### `alignItems`

Use the `alignItems` prop to configure the `align-items` style of the Box (cross-axis alignment).

Available values:
- `BoxAlignItems.Start` - Align items to start of cross axis
- `BoxAlignItems.Center` - Center items on cross axis
- `BoxAlignItems.End` - Align items to end of cross axis
- `BoxAlignItems.Stretch` - Stretch items to fill cross axis
- `BoxAlignItems.Baseline` - Align items to baseline

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>BoxAlignItems</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>undefined</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.AlignItems} />

### `justifyContent`

Use the `justifyContent` prop to configure the `justify-content` style of the Box (main-axis alignment).

Available values:
- `BoxJustifyContent.Start` - Pack items to start of main axis
- `BoxJustifyContent.Center` - Center items on main axis
- `BoxJustifyContent.End` - Pack items to end of main axis
- `BoxJustifyContent.Between` - Distribute items with space between
- `BoxJustifyContent.Around` - Distribute items with space around
- `BoxJustifyContent.Evenly` - Distribute items with equal space

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>BoxJustifyContent</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>undefined</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.JustifyContent} />

### `children`

The content of the Box component.

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>ReactNode</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>undefined</code>
      </td>
    </tr>
  </tbody>
</table>

### `className`

Use the `className` prop to add custom CSS classes to the component. These classes will be merged with the component's default classes using `twMerge`, allowing you to:

- Add new styles that don't exist in the default component
- Override the component's default styles when needed

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>string</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>undefined</code>
      </td>
    </tr>
  </tbody>
</table>

<Canvas of={BoxStories.ClassName} />

### `style`

The `style` prop should primarily be used for dynamic inline styles that cannot be achieved with `className` alone. For static styles, prefer using `className` with Tailwind classes.

<table>
  <thead>
    <tr>
      <th align="left">TYPE</th>
      <th align="left">REQUIRED</th>
      <th align="left">DEFAULT</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td align="left">
        <code>object</code>
      </td>
      <td align="left">No</td>
      <td align="left">
        <code>undefined</code>
      </td>
    </tr>
  </tbody>
</table>

## Component API

<Controls of={BoxStories.Default} />

## References

[MetaMask Design System Guides](https://www.notion.so/MetaMask-Design-System-Guides-Design-f86ecc914d6b4eb6873a122b83c12940)

```tsx
import { Box, BoxFlexDirection, BoxAlignItems } from '@metamask/design-system-react';

<Box
  flexDirection={BoxFlexDirection.Row}
  alignItems={BoxAlignItems.Center}
  gap={4}
  className="p-4 bg-alternative"
>
  Content
</Box>;
```
