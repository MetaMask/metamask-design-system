import { BadgeWrapperPosition } from '@metamask/design-system-shared';

export const AVATAR_BADGE_POSITION = {
  network: BadgeWrapperPosition.BottomRight,
  icon: BadgeWrapperPosition.BottomRight,
  status: BadgeWrapperPosition.TopRight,
} as const;
