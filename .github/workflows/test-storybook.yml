name: Test Storybook

on:
  workflow_call:

jobs:
  test-storybook:
    name: Test Storybook (accessibility & stories) (${{ matrix.label }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: React
            command: yarn test:storybook:react
            retry: 'false'
          - label: React Native
            command: yarn test:storybook:react-native
            retry: 'true'
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@392abd40aa6a0600a3c0ef4af75851662587e6ec # v1.4.0
        with:
          is-high-risk-environment: false
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      - name: Build packages
        run: yarn build
      - name: Run Storybook tests
        if: matrix.retry != 'true'
        run: ${{ matrix.command }}
      - name: Run Storybook tests
        if: matrix.retry == 'true'
        uses: MetaMask/action-retry-command@2377689bfd9a03010a956b7c14273a2fc43d20cf # v1.0.1
        with:
          command: ${{ matrix.command }}
          max-retries: 3
        env:
          NODE_OPTIONS: --max-old-space-size=4096
      - name: Require clean working directory
        shell: bash
        run: |
          if ! git diff --exit-code; then
            echo "Working tree dirty at end of job"
            exit 1
          fi
